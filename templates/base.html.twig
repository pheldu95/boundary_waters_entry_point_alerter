<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Welcome!{% endblock %}</title>
    <link rel="icon" href="{{ asset('images/canoe_logo.png') }}">
    {% block stylesheets %}
    {% endblock %}

    {% block javascripts %}
        {% block importmap %}{{ importmap('app') }}{% endblock %}
    {% endblock %}
</head>
<body class="text-white antialiased" style="background: linear-gradient(135deg, #0d4a3e 0%, #1a5c4d 50%, #0d4a3e 100%);">
    <div class="flex flex-col min-h-screen bg-emerald-900/95">
        <header class="backdrop-blur-sm bg-white/5 border-b border-white/10 shadow-lg sticky top-0 z-50">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                <div class="flex items-center justify-between h-16">
                    <a href="{{ path('app_homepage') }}">
                        <div class="flex items-center space-x-2">
                            <img class="h-[42px]" src="{{ asset('images/canoe_logo.png') }}" alt="canoe logo">
                            <span class="text-xl font-bold bg-gradient-to-r from-white to-amber-200 bg-clip-text text-transparent">
                                Boundary Waters Entry Point Alerter
                            </span>
                        </div>
                    </a>
                    <nav class="hidden sm:flex space-x-8">
                        <a class="text-white/90 hover:text-amber-400 transition-colors duration-200 font-medium" href="{{ path('app_homepage') }}">
                            Home
                        </a>
                        <a class="text-white/90 hover:text-amber-400 transition-colors duration-200 font-medium" href="#">
                            About
                        </a>
                        <a class="text-white/90 hover:text-amber-400 transition-colors duration-200 font-medium" href="#">
                            Contact
                        </a>
                    </nav>

                    <button class="sm:hidden text-white p-2 hover:bg-white/10 rounded-lg transition-colors" id="mobile-menu-button">
                        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
                        </svg>
                    </button>
                </div>

                <nav class="sm:hidden hidden pb-4" id="mobile-menu">
                    <div class="flex flex-col space-y-3">
                        <a class="text-white/90 hover:text-amber-400 transition-colors duration-200 font-medium" href="{{ path('app_homepage') }}">
                            Home
                        </a>
                        <a class="text-white/90 hover:text-amber-400 transition-colors duration-200 font-medium" href="#">
                            About
                        </a>
                        <a class="text-white/90 hover:text-amber-400 transition-colors duration-200 font-medium" href="#">
                            Contact
                        </a>
                    </div>
                </nav>
            </div>
        </header>

        <main class="flex-1">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 flex gap-6">
                <div class="flex-1">
                    {% for message in app.flashes('success') %}
                        <div class="p-4 mb-4 text-green-800 bg-green-100 rounded-lg">
                            {{ message }}
                        </div>
                    {% endfor %}
                    
                    {% for message in app.flashes('error') %}
                        <div class="p-4 mb-4 text-red-800 bg-red-100 rounded-lg">
                            {{ message }}
                        </div>
                    {% endfor %}
                    
                    {% block body %}{% endblock %}
                </div>
                {{ include('main/_asideMenu.html.twig') }}
            </div>
        </main>

        <footer class="mt-auto border-t border-white/10 bg-black/20 backdrop-blur-sm">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
                <div class="text-center">
                    <p class="text-white/70 text-sm">
                        Made by <span class="font-semibold text-white">Duncan Phelps</span>
                    </p>
                    <p class="text-white/50 text-xs mt-2">
                        © {{ "now"|date("Y") }} All rights reserved
                    </p>
                </div>
            </div>
        </footer>
    </div>

    <script>
        document.getElementById('mobile-menu-button')?.addEventListener('click', function() {
            const menu = document.getElementById('mobile-menu');
            menu.classList.toggle('hidden');
        });
    </script>
</body>
</html>